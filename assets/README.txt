Place your logo file here and name it exactly:

logo.png

The website header is already set up to load `/assets/logo.png`.
If the file is missing, it will show a simple text fallback ("WE CARE").

If your file is a JPG instead, rename it to `logo.png` after exporting a PNG copy,
or ask for an update and the code can be changed to `.jpg`.
